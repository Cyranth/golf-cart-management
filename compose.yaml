services:
  backend:
    restart: on-failure
    build:
      context: ./backend
      dockerfile: .docker/Dockerfile
      args:
        - BASE_IMAGE_TAG=${BASE_IMAGE_TAG:-22-bullseye-slim}
    ports:
      - "3000:3000"
    volumes:
      - ./backend:/app
      - /app/node_modules
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
